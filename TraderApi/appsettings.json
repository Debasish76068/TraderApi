{
  "Logging": {
    "LogLevel": { // All providers, LogLevel applies to all the enabled providers.
      "Default": "Information", // Default logging, Error and higher.
      "Microsoft.AspNetCore": "Error" // All Microsoft* categories, Warning and higher.
    },
    "Console": { // Console provider.
      "LogLevel": {
        "Default": "Information", // Overrides preceding LogLevel:Default setting.
        "Microsoft.Hosting": "Trace" // Debug:Microsoft.Hosting category.
      }
    },
    "Debug": { // Debug provider.
      "LogLevel": {
        "Default": "Information", // Overrides preceding LogLevel:Default setting.
        "Microsoft.Hosting": "Error" // Debug:Microsoft.Hosting category.
      }
    }
  },
  "AllowedHosts": "*",
  "TraderApiKey": "pgH7QzFHJx4w46fI~5Uzi4RvtTwlEXp",
  "ConnectionStrings": {
    "TraderApiContext": "Server=(localdb)\\Local;Database=Trader;Trusted_Connection=True;MultipleActiveResultSets=true"
  }
}
    